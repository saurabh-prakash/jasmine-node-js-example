---
framework: jasmine
preRun: 
  command: 
    - npm ci   
preMerge:
  pattern:
    - "spec/**/*.spec.js"
postMerge:
  pattern:
    - "spec/**/*.spec.js"
postRun:
  command: 
    - echo 'Done Testing by SP'
parallelism: 4
version: 1.0
